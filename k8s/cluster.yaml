apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: blinkit-cluster
  region: us-east-1
  version: "1.30"

ManagedNodeGroups:
  - name: blinkit-workers
    instanceType: t2.medium
    minSize: 1
    maxSize: 3
    desiredCapacity: 2
    volumeSize: 20